# Edge-camera-02 API retest — 2026-09-08 20:12 WITA

Target: `http://10.60.20.96:3000`. Raw sanitized evidence: [JSON](edge-camera-02-test-2026-09-08-2012.json).

| Check | Result |
| --- | --- |
| Health | HTTP 200, status ok, agent 0.1.0 (387 ms) |
| Device identity | HTTP 200, edge-camera-02 (64 ms) |
| Camera state | disconnected; connected=false; no model/serial/capabilities |
| Camera status | HTTP 200, disconnected; storage/settings empty (53 ms) |
| Create lease | HTTP 201 (139 ms) |
| Renew lease | HTTP 200 (27 ms) |
| Device after lease | Still disconnected (65 ms) |
| Preview | HTTP 502, CAMERA_COMMAND_FAILED, gphoto2 error -52: USB device not found (60 ms) |
| Release lease | HTTP 204 (82 ms) |
| Autofocus/capture/job/media | Not attempted: camera/preview prerequisite failed |

Conclusion: HTTP API and lease lifecycle are functional from this test host. Physical camera operations remain blocked because the edge camera command cannot find its USB device. This evidence does not distinguish power/cable/USB enumeration, service USB visibility, or stale device configuration. Inspect the camera connection and edge service USB access/logs before repeating capture tests.

All test leases were released. No registry record was recreated, no database/configuration change was made, no service was restarted, and no capture command or application finalization was sent. Existing edge-02 reports remain historical. No application code/OpenAPI or mobile roadmap phase change is required by this direct API diagnostic.
