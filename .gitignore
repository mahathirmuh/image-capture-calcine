# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Lovable platform scaffold metadata (project no longer uses Lovable)
.lovable/

# Env / secrets
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code: preferensi mesin ini saja (memuat hook auto-commit).
# Skrip .claude/auto-commit-push.sh sengaja ikut ter-commit; tanpa berkas
# di bawah ini skrip itu tidak pernah dipanggil, jadi clone orang lain
# tidak tiba-tiba auto-push.
.claude/settings.local.json
